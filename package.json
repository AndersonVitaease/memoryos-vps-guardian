{
  "name": "memoryos-vps-guardian",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Public MVP: engineering.vps.health MCP tool (read-only, deterministic, evidence-first).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
